# Example environment configuration for Data Porter Wrapper

# Base URL for Data Porter (can be local mock or real Data Porter service)
DATA_PORTER_BASE_URL=http://host.docker.internal:5001

# JWT token for authenticating with Data Porter
DATA_PORTER_JWT=your-dev-jwt-token

# Optional timeout (in ms) for HTTP requests
HTTP_TIMEOUT_MS=15000